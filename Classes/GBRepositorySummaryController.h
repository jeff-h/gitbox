#import "GBRepositorySettingsViewController.h"

// Despite the legacy class name, this controller now backs the "Ignored Files" tab.
// (The class isn't renamed to avoid touching the Xcode project file.)
@interface GBRepositorySummaryController : GBRepositorySettingsViewController
@end
